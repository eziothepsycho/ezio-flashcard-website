<?php

/*
|--------------------------------------------------------------------------
| Web routes
|--------------------------------------------------------------------------
|
| This backend is API-only: the website is the separate Vite/React app in the
| project root, and everything this app serves lives in routes/api.php (plus the
| framework's health route at /up).
|
| Laravel's default `GET /` welcome view used to live here. It was removed along
| with the frontend scaffolding it depended on — see backend/README.md. The file
| itself stays because bootstrap/app.php registers it as the `web` routes.
|
*/
